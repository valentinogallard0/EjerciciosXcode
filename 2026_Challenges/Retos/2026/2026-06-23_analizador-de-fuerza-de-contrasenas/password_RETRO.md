# Retro — Analizador de fuerza de contraseñas

> Se llena cuando termines y pidas revisión: **"Listo, revisa mi reto del analizador"**.

## Qué se evaluará

| Área | Qué se espera |
|---|---|
| Lógica pura | `analyze` es función pura, mismo input → mismo output |
| Modelado | `PasswordStrength` y `PasswordRule` como `enum`, estados imposibles imposibles |
| Separación | la View no calcula nada, solo lee `PasswordReport` |
| Reglas | cada regla aislada y clara; el score se justifica |
| Edge cases | vacío, repeticiones y secuencias comunes cubiertos |
| Criterio | sabes explicar **por qué** combinaste las reglas así |

## Feedback

_(pendiente)_
